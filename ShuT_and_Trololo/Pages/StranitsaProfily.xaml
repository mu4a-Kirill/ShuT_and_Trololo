<Page x:Class="ShuT_and_Trololo.Pages.StranitsaProfily"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#1E1E2E">

    <Page.Resources>
        <Style TargetType="Button" x:Key="BtnFiolet">
            <Setter Property="Background" Value="#7C3AED"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6D28D9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="BtnKrasn">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">

            <TextBlock Text="Мой профиль"
                       FontSize="24" FontWeight="Bold"
                       Foreground="#C4B5FD" Margin="0,0,0,24"/>

            <Border x:Name="panelZamorozka"
                    Background="#7F1D1D"
                    CornerRadius="8"
                    Padding="20" Margin="0,0,0,20"
                    Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="❄ Ваш аккаунт заморожен"
                               FontSize="16" FontWeight="Bold"
                               Foreground="#FCA5A5" Margin="0,0,0,8"/>
                    <TextBlock x:Name="txtPrichinaZam"
                               FontSize="13" Foreground="#FEE2E2"
                               TextWrapping="Wrap" Margin="0,0,0,12"/>
                    <Button Content="Оспорить заморозку"
                            Style="{StaticResource BtnKrasn}"
                            HorizontalAlignment="Left"
                            Click="btnOsporit_Click"/>
                </StackPanel>
            </Border>

            <Border Background="#2D2D3F" CornerRadius="10"
                    Padding="24" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="90" Height="90"
                            CornerRadius="45"
                            Background="#4C1D95">
                        <Image x:Name="imgAvatar"
                               Stretch="UniformToFill"
                               RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Clip>
                                <EllipseGeometry RadiusX="45" RadiusY="45"
                                                 Center="45,45"/>
                            </Image.Clip>
                        </Image>
                    </Border>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock x:Name="txtImya"
                                   FontSize="20" FontWeight="Bold"
                                   Foreground="White" Margin="0,0,0,6"/>
                        <TextBlock x:Name="txtLogin"
                                   FontSize="13" Foreground="#A78BFA"
                                   Margin="0,0,0,4"/>
                        <TextBlock x:Name="txtEmail"
                                   FontSize="13" Foreground="#9CA3AF"
                                   Margin="0,0,0,4"/>
                        <TextBlock x:Name="txtRol"
                                   FontSize="13" Foreground="#6EE7B7"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Button x:Name="btnZayavkaAvtor"
                    Content="📝 Подать заявку на роль Автора"
                    Style="{StaticResource BtnFiolet}"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,24"
                    Visibility="Collapsed"
                    Click="btnZayavkaAvtor_Click"/>

            <Separator Background="#3D3D5F" Margin="0,0,0,24"/>

            <TextBlock Text="Мои отзывы"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="#C4B5FD" Margin="0,0,0,16"/>

            <StackPanel x:Name="panelOtzyvy"/>

        </StackPanel>
    </ScrollViewer>
</Page>